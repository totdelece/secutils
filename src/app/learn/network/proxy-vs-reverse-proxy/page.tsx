import Link from "next/link";
import { ArticleLayout } from "../../_components/ArticleLayout";
import { getArticle } from "@/lib/articles";

const article = getArticle("network", "proxy-vs-reverse-proxy")!;

export default function Page() {
  return (
    <ArticleLayout article={article}>
      <p>
        「プロキシ」と「リバースプロキシ」は名前が似ていますが、<strong>置く人も、守る対象も逆</strong>です。フォワードプロキシはクライアント側が置き、リバースプロキシはサーバー側が置きます。この1点さえ押さえておけば、設定ファイルがどこにあるのか、障害のときにどこから疑うのか、アクセスログのIPアドレスをなぜそのまま信用してはいけないのかが、一本の線でつながります。
      </p>
      <p>
        この記事ではまず RFC の定義で両者を整理し、次に<strong>実際に本番サイトのレスポンスヘッダーを見てリバースプロキシの痕跡を確認</strong>します。さらに20行ほどのプロキシを書いて <code>X-Forwarded-For</code> がどう変化するかを実測し、最後に502／504の切り分けやリダイレクトループなど、実務でハマる点をまとめます。
      </p>

      <h2>結論：どちら側に置くかが逆</h2>
      <table>
        <thead>
          <tr>
            <th></th>
            <th>フォワードプロキシ（いわゆるプロキシ）</th>
            <th>リバースプロキシ</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><strong>置く人</strong></td>
            <td>クライアント側（社内ネットワークの管理者・利用者）</td>
            <td>サーバー側（サイトの運営者）</td>
          </tr>
          <tr>
            <td><strong>誰の代理か</strong></td>
            <td>クライアントの代理として外に出ていく</td>
            <td>サーバーの代理としてリクエストを受ける</td>
          </tr>
          <tr>
            <td><strong>相手から見た姿</strong></td>
            <td>多数の利用者がまとめられた1つの送信元</td>
            <td>そのサイト本体（オリジンに見える）</td>
          </tr>
          <tr>
            <td><strong>設定の置き場所</strong></td>
            <td>ブラウザ・OS・PACファイルなどクライアント側</td>
            <td>サーバー前段の設定（nginx等）やCDNの管理画面</td>
          </tr>
          <tr>
            <td><strong>存在に気づくか</strong></td>
            <td>利用者は自分で設定するので知っている</td>
            <td>利用者は普通、存在に気づかない</td>
          </tr>
          <tr>
            <td><strong>主な目的</strong></td>
            <td>出口の制御・フィルタリング・監査・匿名化</td>
            <td>TLS終端・キャッシュ・負荷分散・防御</td>
          </tr>
        </tbody>
      </table>
      <p>
        HTTPの仕様である RFC 9110 は、通信の途中に入る装置（中間装置）を3つに分類しています。
      </p>
      <ul>
        <li><strong>proxy</strong>：クライアントが選んだ、メッセージを転送するエージェント（3.7節）</li>
        <li><strong>gateway（別名 reverse proxy）</strong>：<strong>外向きの接続に対してはオリジンサーバーとして振る舞い</strong>、受け取ったリクエストを内側の別のサーバーへ転送する中間装置（3.7節）</li>
        <li><strong>tunnel</strong>：2つの接続の間をメッセージを変えずに中継するだけの存在。動き出したあとは通信の当事者とみなされない（3.7節）</li>
      </ul>
      <p>
        注目したいのは、RFCがリバースプロキシを <strong>gateway</strong> と呼び、「外向きの接続に対してはオリジンサーバーとして振る舞う」と定義していることです。RFCは「オリジンサーバーに適用されるHTTPの要件は、すべてゲートウェイの外向き通信にも適用される」とまで書いています。<strong>クライアントから見れば、リバースプロキシはそのサイトそのもの</strong>だということです。この後の実測でも、まさにそのとおりの挙動が観測できます。
      </p>

      <h2>フォワードプロキシ：クライアントが選んで置く</h2>
      <p>
        RFCの定義にあるとおり、プロキシは<strong>クライアントが選ぶ</strong>ものです。だから設定はクライアント側にあります。「自分の環境だけ特定のサイトに繋がらない」というトラブルで、まずブラウザやOSのプロキシ設定、PACファイルを疑うのはこのためです。
      </p>

      <h3>何のために置くのか</h3>
      <ul>
        <li><strong>出口の一点集約</strong>：外部への通信を1か所に通し、許可カテゴリや宛先で遮断する</li>
        <li><strong>記録</strong>：誰がいつどこへアクセスしたかのログを残す（監査・インシデント調査の基礎データ）</li>
        <li><strong>検査</strong>：ダウンロードファイルのマルウェアスキャンなど</li>
        <li><strong>キャッシュ</strong>：同じコンテンツを複数の利用者で共有し、回線を節約する（回線が細かった時代の主目的）</li>
        <li><strong>送信元IPの隠蔽</strong>：外部から見える送信元をプロキシのIPに統一する</li>
      </ul>
      <p>
        通信を止めるという点では{" "}
        <Link href="/learn/network/firewall-basics">ファイアウォールの基本</Link>{" "}
        と目的が重なりますが、レイヤーが違います。ファイアウォールは主にIPアドレスとポートで通す・通さないを判断し、フォワードプロキシは<strong>HTTPの中身を理解したうえで</strong>URLやメソッド単位の判断ができます。両方を組み合わせて「外に出るときは必ずプロキシ経由、それ以外の直接通信はファイアウォールで遮断」という構成が定番です。
      </p>

      <h3>HTTPSの中身は見えない：CONNECT というトンネル</h3>
      <p>
        HTTPのリクエストならプロキシは中身を読めますが、HTTPSは暗号化されています。ではどうやって中継するのか。ここで使われるのが <code>CONNECT</code> メソッドです。
      </p>
      <pre><code>{`CONNECT example.com:443 HTTP/1.1
Host: example.com:443
Proxy-Authorization: basic ...`}</code></pre>
      <p>
        クライアントは接続したいホストとポートだけを書いた形式（authority-form）でトンネルの開設を要求します。<strong>ポートの省略はできません</strong>。2xx系の応答が返るとプロキシはトンネルモードに入り、以降は中身を解釈せずにTCPのバイト列を双方向に流すだけになります。つまり<strong>暗号化された中身はプロキシからは見えません</strong>。
      </p>
      <p>
        企業がHTTPSの中身までフィルタしたい場合に「TLSインスペクション」（社内の認証局証明書を端末に配布し、プロキシでいったん復号して検査し、再暗号化する）が必要になるのは、この制約があるためです。証明書の検証がどう成立しているかは{" "}
        <Link href="/learn/network/https-tls">HTTPSとTLSの仕組み</Link>{" "}
        で扱っています。会社のPCだけ証明書の発行者が社内CAになっている、という現象の正体はこれです。
      </p>
      <p>
        なお、認証が必要なプロキシは <strong>407 Proxy Authentication Required</strong> を返し、クライアントは <code>Proxy-Authorization</code> ヘッダーを付けて再送します。401（サーバー自身の認証）と紛らわしいですが、<strong>407が返ったら相手はサーバーではなく途中のプロキシ</strong>だと判断できます。
      </p>
      <p>
        MDNは、CONNECTの接続先を既知のポートや許可リストに制限すべきだと注意しています。誰でも使えるオープンプロキシを放置すると、スパムの中継のような踏み台に使われるためです。内部にあるプロキシが外から使える状態になっていると、外部からの攻撃で内部リソースへ到達する経路にもなります。この構図は{" "}
        <Link href="/learn/security/ssrf">SSRF（サーバーサイドリクエストフォージェリ）</Link>{" "}
        と本質的に同じ問題です。
      </p>

      <h2>リバースプロキシ：サーバー側が黙って置く</h2>
      <p>
        リバースプロキシはクライアントが選びません。DNSがリバースプロキシを指しているので、利用者は「サイトに接続した」としか思っていません。それでいて、実際にリクエストを最初に受け取っているのはアプリケーションではなくこの前段です。
      </p>
      <p>
        アプリの前に1台置くだけで、次のことがまとめて手に入ります。
      </p>
      <ul>
        <li><strong>TLS終端</strong>：証明書の管理と更新をここに集約する。背後のアプリはHTTPだけ喋ればよくなる</li>
        <li><strong>キャッシュと静的配信</strong>：画像やビルド済みHTMLを前段が返し、アプリに触れさせない</li>
        <li><strong>負荷分散・冗長化</strong>：複数のアプリサーバーへ振り分け、応答しなくなったものを外す</li>
        <li><strong>ルーティング</strong>：<code>/api</code> はアプリへ、それ以外は静的ファイルへ、のようにパスで振り分ける</li>
        <li>
          <strong>防御の一枚目</strong>：レート制限、WAF、セキュリティヘッダーの付与（
          <Link href="/learn/security/http-security-headers">HTTPセキュリティヘッダー</Link>
          ）
        </li>
        <li>
          <strong>圧縮・プロトコル変換</strong>：外はHTTP/2やHTTP/3、内側はHTTP/1.1、といった使い分け（
          <Link href="/learn/network/http-versions">HTTPバージョンの違い</Link>
          ）
        </li>
      </ul>

      <h2>実測：このサイトのレスポンスにも痕跡がある</h2>
      <p>
        言葉で説明するより、実物を見たほうが早いです。本サイト（Vercelでホスティング）のページに <code>curl</code> でヘッダーだけを取得すると、次の応答が返りました（2026年8月30日実測、抜粋）。
      </p>
      <pre><code>{`$ curl -sSI https://secutils.jp/learn/network/dns-basics

HTTP/1.1 200 OK
Cache-Control: public, max-age=0, must-revalidate
Age: 1206
Etag: "cb0352485eefbaa78e9eb7ea5f58f464"
Server: Vercel
X-Vercel-Cache: HIT
X-Vercel-Id: kix1::wbkhp-1788056830566-4b2d8503aef5`}</code></pre>
      <p>読み取れることが3つあります。</p>
      <ul>
        <li>
          <strong><code>Server: Vercel</code></strong> ── 応答を返しているのは、私が書いたアプリケーションではなく<strong>前段のプラットフォーム</strong>です。RFCの言う「外向きにはオリジンサーバーとして振る舞う」がそのまま現れています。
        </li>
        <li>
          <strong><code>X-Vercel-Cache: HIT</code> と <code>Age: 1206</code></strong> ── 約20分前に生成された応答が<strong>キャッシュから返っています</strong>。このリクエストではアプリのコードは1行も動いていません。RFC 9111 は <code>Age</code> を「オリジンサーバーが応答を生成または検証してから経過したと送信側が推定する秒数」と定義しています（5.1節）。
        </li>
        <li>
          <strong><code>X-Vercel-Id: kix1::…</code></strong> ── 応答した拠点を示す識別子です。同じ時刻に旧ドメイン（<code>secutils.vercel.app</code>）へリクエストしたときは <code>hnd1::…</code> という別拠点で、しかも <strong>308 Permanent Redirect が前段から返っていました</strong>。リダイレクトのためにアプリまで到達する必要すらないわけです。
        </li>
      </ul>
      <p>
        ここで言うキャッシュは、RFC 9111 が<strong>共有キャッシュ</strong>と呼ぶものです。同じ仕様は、利用者ひとり専用の<strong>プライベートキャッシュ</strong>（ブラウザのキャッシュなど）と対比して、共有キャッシュを「複数の利用者のために応答を再利用して保存するもので、通常は中間装置の一部として配置される」と定義しています（1.3節）。<strong>「誰に配られても問題ない応答か」を意識しないといけないのは、この共有という性質から来ています。</strong>後述するキャッシュ事故の話につながります。
      </p>

      <h2>前段を挟むと、クライアントの情報が消える</h2>
      <p>
        リバースプロキシを挟むと、アプリから見た接続元は<strong>常にプロキシのIPアドレス</strong>になります。素朴にソケットの接続元を取得すると、全アクセスが同じIPに見えてしまいます。
      </p>
      <p>
        そこで、消えてしまう情報を前段がヘッダーに詰めて渡す慣習が生まれました。
      </p>
      <table>
        <thead>
          <tr>
            <th>ヘッダー</th>
            <th>渡すもの</th>
            <th>位置づけ</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code>X-Forwarded-For</code></td>
            <td>元のクライアントのIPアドレス</td>
            <td>事実上の標準（仕様書はない）</td>
          </tr>
          <tr>
            <td><code>X-Forwarded-Proto</code></td>
            <td>クライアントが使ったプロトコル（http / https）</td>
            <td>同上</td>
          </tr>
          <tr>
            <td><code>X-Forwarded-Host</code></td>
            <td>クライアントが要求した元のホスト名</td>
            <td>同上</td>
          </tr>
          <tr>
            <td><code>Forwarded</code></td>
            <td>上記をまとめた標準形（<code>for=</code> / <code>by=</code> / <code>proto=</code> / <code>host=</code>）</td>
            <td>RFC 7239 で標準化</td>
          </tr>
          <tr>
            <td><code>Via</code></td>
            <td>経路上のプロキシ自身の情報</td>
            <td>RFC 9110。クライアントではなくプロキシについての情報</td>
          </tr>
        </tbody>
      </table>
      <p>
        標準は <code>Forwarded</code> ですが、実際の現場では <code>X-Forwarded-For</code> が圧倒的に使われています。RFC 7239 自身も、標準化以前から <code>X-Forwarded-For</code> などの非標準ヘッダーが使われてきたと認めたうえで、相互運用性のための標準形を定義する、という立て付けになっています。
      </p>

      <h3>実験：20行のプロキシで挙動を確かめる</h3>
      <p>
        「<code>X-Forwarded-For</code> は偽装できる」という話は有名ですが、実際にどう見えるのかを確かめたことがある人は多くないと思います。この記事のために、最小のリバースプロキシとオリジンサーバーをNode.jsで書いて手元で動かしました。プロキシの中核はこれだけです。
      </p>
      <pre><code>{`const proxy = http.createServer((creq, cres) => {
  const prior = creq.headers["x-forwarded-for"];   // 既に付いている値
  const client = creq.socket.remoteAddress;        // 実際の接続元
  const headers = {
    ...creq.headers,
    // 既存の値があれば「追記」する（多くの実装がこうする）
    "x-forwarded-for": prior ? prior + ", " + client : client,
    "x-forwarded-proto": "https",
  };
  // ここでオリジンへ転送する
});`}</code></pre>
      <p>
        オリジン側は受け取ったヘッダーをそのまま返すだけにしてあります。まず普通にリクエストした場合。
      </p>
      <pre><code>{`{
  "remoteAddress": "127.0.0.1",
  "host": "origin.internal:3999",
  "xff": "127.0.0.1",
  "xfproto": "https"
}`}</code></pre>
      <p>
        次に、<strong>クライアントが自分で <code>X-Forwarded-For: 1.2.3.4</code> を付けて</strong>同じプロキシに送った場合。
      </p>
      <pre><code>{`{
  "remoteAddress": "127.0.0.1",
  "host": "origin.internal:3999",
  "xff": "1.2.3.4, 127.0.0.1",
  "xfproto": "https"
}`}</code></pre>
      <p>
        <strong>偽の値 <code>1.2.3.4</code> は消えずに左端に残り、プロキシが観測した本物のIPはその右に追記されました。</strong>もしアプリが「<code>X-Forwarded-For</code> の左端＝クライアントIP」という素直な実装をしていたら、このリクエストのクライアントIPは <code>1.2.3.4</code> として記録されます。攻撃者は好きなIPアドレスを名乗れることになります。
      </p>
      <p>
        これは私の書いたコードが特殊なのではありません。nginx の公式ドキュメントは、よく使われる変数 <code>$proxy_add_x_forwarded_for</code> を「クライアントリクエストの <code>X-Forwarded-For</code> に <code>$remote_addr</code> をカンマ区切りで<strong>追記</strong>した値」と定義しています。多くの構成例がこの変数をそのまま使っているため、<strong>外から来た嘘の値も一緒に引き継がれます</strong>。
      </p>
      <p>
        RFC 7239 のセキュリティ考慮事項も同じことを警告しています。「<code>Forwarded</code> ヘッダーフィールドは正しいものとして信頼できない。サーバーに至るまでのすべてのノードが、誤って、あるいは悪意を持って変更しうるからである」。
      </p>

      <h3>では何を信用すればよいのか</h3>
      <p>
        原則はひとつです。<strong>自分が管理している最前段のプロキシが書いた値だけを信用する。</strong>具体的には次の2段構えにします。
      </p>
      <ul>
        <li>
          <strong>入口では追記ではなく上書きする</strong>。nginx なら <code>proxy_set_header X-Forwarded-For $remote_addr;</code> と書けば、外から届いた値は捨てられます。信頼境界の一番外側では、持ち込まれた値を必ず洗い流します。
        </li>
        <li>
          <strong>アプリ側では信頼するプロキシを限定する</strong>。Express なら <code>trust proxy</code> の設定に、ホップ数やIPアドレス／サブネットを指定します。公式ドキュメントは、これを <code>true</code>（全面的に信頼）にするなら<strong>前段が <code>X-Forwarded-*</code> を必ず上書きしていることが前提</strong>だと明記し、ホップ数で指定する場合も「アプリへ至る経路すべてでホップ数が同じでないと、ホップの少ない経路から詐称できる」と注意しています。
        </li>
      </ul>
      <p>
        なぜここまで気にするかというと、<strong>IP制限・レート制限・地域判定・監査ログ・不正検知が、すべてこの1つの値に依存している</strong>からです。ここが偽装できると、レート制限はリクエストごとにIPを変えるだけで無限に回避され、インシデント調査で頼りにする監査ログには嘘のIPアドレスが残ります。IPアドレスの表記を確認したいときは{" "}
        <Link href="/tools/ip-converter">IP Address Converter</Link>{" "}
        や{" "}
        <Link href="/tools/cidr-calculator">CIDR Calculator</Link>{" "}
        が使えます（
        <Link href="/learn/network/cidr-notation">CIDR表記の読み方</Link>
        も参考にしてください）。
      </p>
      <p>
        CDNやPaaSを使っている場合は、そのプラットフォームが<strong>改ざんできない形で提供している値</strong>（プラットフォーム固有のヘッダーや実行環境のAPI）があるかをドキュメントで確認し、あればそちらを優先してください。自分で管理していない前段の <code>X-Forwarded-For</code> を素通しで信用するより確実です。
      </p>

      <h2>実務でハマるところ</h2>

      <h3>502と504は「前段からの信号」</h3>
      <p>
        <strong>502 Bad Gateway</strong> はゲートウェイやプロキシが上流から不正な応答を受け取ったことを、<strong>504 Gateway Timeout</strong> は上流の応答を待ってタイムアウトしたことを示します。どちらも共通して意味しているのは、<strong>前段は生きていて、その奥のオリジンが応答できていない</strong>ということです。
      </p>
      <p>
        したがって502／504がブラウザに出たときの切り分けは、①アプリのプロセスは生きているか ②前段からアプリへ到達できるか（ポート・ネットワーク）③前段のタイムアウト設定を超えていないか、の順になります。逆に、アプリが自力で返す500との違いを意識すると、どちらのログを見に行くべきかで迷わなくなります。ステータスコードの意味をまとめて確認したいときは{" "}
        <Link href="/tools/http-status">HTTP Status Code Reference</Link>{" "}
        を使ってください。
      </p>

      <h3>タイムアウトの不一致で「正常なのに504」</h3>
      <p>
        nginx の <code>proxy_read_timeout</code> の既定値は<strong>60秒</strong>です。アプリ側に90秒かかる処理があると、アプリは正常に処理を続けているにもかかわらず、利用者には504が返ります。しかもアプリのログにはエラーが残らないため、原因にたどり着きにくい種類の障害です。重い処理を持つなら、前段のタイムアウト値も必ずセットで確認します（あるいは、そもそも同期処理から切り離します）。
      </p>

      <h3>Hostヘッダーが書き換わって生成URLが壊れる</h3>
      <p>
        nginx は既定で、元のリクエストの <code>Host</code> をそのまま渡しません。公式ドキュメントによれば、既定は転送先のホスト名（<code>$proxy_host</code>）に書き換えられます。アプリが絶対URLを生成していると、リダイレクト先やcanonical、メール本文中のリンクが<strong>内部のホスト名になってしまいます</strong>。<code>proxy_set_header Host $host;</code> を明示するのが定石です。
      </p>

      <h3>TLS終端によるリダイレクトループ</h3>
      <p>
        前段でTLSを終端すると、アプリにはHTTPで届きます。ここでアプリが「HTTPで来たらHTTPSへリダイレクト」を素直に実装していると、外側はHTTPSなのにアプリは毎回HTTPだと判断し、<strong>無限にリダイレクトし続けます</strong>。判定には <code>X-Forwarded-Proto</code> を見る必要があり、そのためにフレームワーク側で「前段を信頼する」設定（前述の <code>trust proxy</code> 等）を入れます。リダイレクトループを見たら、まずこの組み合わせを疑ってください。
      </p>

      <h3>共有キャッシュに個人向けの応答が入る</h3>
      <p>
        前段のキャッシュは<strong>複数の利用者で共有される</strong>ため、ログイン後のページが入り込むと他人に配られます。RFC 9111 は、<code>Authorization</code> ヘッダーを含むリクエストへの応答について、共有キャッシュがそれを再利用してよいのは <code>must-revalidate</code> / <code>public</code> / <code>s-maxage</code> のような明示的なディレクティブがある場合に限る、と定めています（3.5節）。
      </p>
      <p>
        また、同じURLでも言語や端末で内容が変わるなら <code>Vary</code> の指定が必要です。RFC 9111 は、<code>Vary</code> に挙げられた要求ヘッダーが元のリクエストと一致しない限り、保存された応答を検証なしに使ってはならないと定めています（4.1節）。<strong>「キャッシュは前段の仕事だから」とアプリ側が無関心でいると事故になります。</strong>キャッシュしてほしくない応答には、アプリ側から <code>Cache-Control: private</code> や <code>no-store</code> を明示してください。
      </p>

      <h3>リクエストスマグリング：前段と後段の解釈のズレ</h3>
      <p>
        前段と後段でリクエストの区切り方の解釈が食い違うと、1つの接続に紛れ込ませた別のリクエストが後段にだけ見える、という攻撃が成立します。原因になりやすいのが、ボディの長さの指定が2通りある点です。
      </p>
      <p>
        RFC 9112 は、<code>Transfer-Encoding</code> と <code>Content-Length</code> の両方があるメッセージを受け取った場合、<strong><code>Transfer-Encoding</code> が <code>Content-Length</code> を上書きする</strong>と定めています（6.3節）。中間装置は転送する前に受け取った <code>Content-Length</code> を除去して処理すべきだ、とも書かれています。同じ仕様のセキュリティ考慮事項（11.2節）は、リクエストスマグリングが「複数の受信者間の解釈の違いを悪用して追加のリクエストを隠すもの」であり、6.3節のメッセージ区切りに関する要件はその有効性を下げるために導入された、と説明しています。
      </p>
      <p>
        実務上の対策はシンプルで、<strong>前段と後段のHTTP実装を新しく保つこと</strong>、そして古いプロキシと新しいアプリサーバーのような世代の違う組み合わせを避けることです。片方だけ更新して安心しない、という点だけ覚えておいてください。
      </p>

      <h2>目的別：どちらを使うか</h2>
      <table>
        <thead>
          <tr>
            <th>やりたいこと</th>
            <th>使うもの</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>社員の外向き通信を制御・記録したい</td>
            <td>フォワードプロキシ</td>
          </tr>
          <tr>
            <td>取引先に対して固定の送信元IPで出たい</td>
            <td>
              フォワードプロキシ（または
              <Link href="/learn/network/nat-port-forwarding">NAT</Link>
              の設計）
            </td>
          </tr>
          <tr>
            <td>1台のサーバーで複数のサイト・アプリを配信したい</td>
            <td>リバースプロキシ（ホスト名／パスで振り分け）</td>
          </tr>
          <tr>
            <td>証明書の管理を1か所にまとめたい</td>
            <td>リバースプロキシ（TLS終端）</td>
          </tr>
          <tr>
            <td>アクセス集中に耐えたい</td>
            <td>リバースプロキシ＋キャッシュ／負荷分散</td>
          </tr>
          <tr>
            <td>自宅サーバーやVPSを安全に公開したい</td>
            <td>
              <Link href="/learn/network/nat-port-forwarding">ポート開放</Link>
              ＋リバースプロキシ（アプリを直接外に出さない）
            </td>
          </tr>
          <tr>
            <td>通信内容を秘匿したい・別拠点のネットワークに入りたい</td>
            <td>
              <Link href="/learn/network/vpn-basics">VPN</Link>
            </td>
          </tr>
        </tbody>
      </table>
      <p>
        最後の行に関連して、プロキシとVPNの違いもここで整理しておきます。<strong>プロキシはアプリケーション層で特定のプロトコル（多くはHTTP）を中継する</strong>仕組みで、中身を理解して手を加えられます。<strong>VPNはネットワーク層で経路ごと包む</strong>仕組みで、その中を流れるのがHTTPだろうがSSHだろうが関知しません。「ブラウザの通信だけ会社の出口を通したい」ならプロキシ、「端末の通信すべてを別のネットワークに置きたい」ならVPN、という選び分けになります。
      </p>

      <h2>リバースプロキシを置くときのチェックリスト</h2>
      <ul>
        <li>☐ <code>Host</code> を明示的に転送している（既定のままにしていない）</li>
        <li>☐ 最前段で <code>X-Forwarded-For</code> を<strong>上書き</strong>している（追記のままにしていない）</li>
        <li>☐ アプリ側で信頼するプロキシを限定している（無条件に信頼していない）</li>
        <li>☐ HTTPSへのリダイレクト判定に <code>X-Forwarded-Proto</code> を使っている</li>
        <li>☐ 前段とアプリのタイムアウトの大小関係を把握している</li>
        <li>☐ 認証が必要な応答が共有キャッシュに入らない設定になっている</li>
        <li>☐ 要求ヘッダーで内容が変わる応答に <code>Vary</code> を指定している</li>
        <li>☐ セキュリティヘッダーを前段とアプリのどちらが付けるか決め、重複していない</li>
        <li>☐ 502／504が出たときに、すぐオリジンのログを見られる導線がある</li>
        <li>☐ 前段・後段のHTTP実装を更新している（リクエストスマグリング対策）</li>
      </ul>
      <p>
        最終的に外へ出ているヘッダーの状態は、レスポンスヘッダーを貼り付けて採点できる{" "}
        <Link href="/tools/security-headers">Security Headers Analyzer</Link>{" "}
        で確認できます。前段とアプリの両方でヘッダーを設定していると、片方が上書きしていたり二重に付いていたりすることがあるので、<strong>設定ファイルではなく実際の応答で確認する</strong>のが確実です。
      </p>

      <h2>実体験：自分のサイトの前にも他人のプロキシが立っていた</h2>
      <p>
        本サイトを作り始めたころ、リバースプロキシは「大きなシステムの構成図に出てくるもの」だと思っていました。実際には、Vercelにデプロイした時点で<strong>自分のアプリの前には既にリバースプロキシが立っていた</strong>わけですが、そのことを意識したのは、この記事のために自分のサイトへ <code>curl</code> を打ったときでした。<code>Server: Vercel</code>、<code>X-Vercel-Cache: HIT</code>、<code>Age: 1206</code>。<strong>自分が書いたコードが1行も動いていない状態で、正しいページが返っている</strong>という事実は、頭で理解しているのと画面で見るのとでは受け取り方がかなり違いました。旧ドメインの308リダイレクトが前段から返っていたのも同様です。
      </p>
      <p>
        「どこが応答しているのか」を取り違える失敗も経験しました。記事を1本追加してビルドし直したのに、ブラウザでは404のまま。原因は、前に起動していた開発サーバーのプロセスがポートを掴んだまま残っていて、<strong>新しいサーバーの起動が失敗し、古いサーバーが応答し続けていた</strong>ことでした。コードを何度見直しても直らないはずです。この一件以来、まず「いま応答しているのは自分が思っている相手か」を確認するようになりました。前段が入ると、この種の取り違えは確実に増えます。
      </p>
      <p>
        そして今回、記事のために20行ほどのプロキシを書いて <code>X-Forwarded-For</code> を観察しました。「偽装できる」という知識自体は前からありましたが、<code>1.2.3.4, 127.0.0.1</code> という文字列を実際に目にすると、この値を素直に「クライアントIP」としてログに書いている実装がどれだけ危ういかが腹に落ちました。<strong>信用できるのは、自分が管理している最前段が書いた1つだけ</strong>。これは実際に手を動かして初めて実感できた部分です。
      </p>
      <p>
        もう一つ、セキュリティヘッダーの設定で気づいたことがあります。本サイトのヘッダーは <code>next.config.ts</code> に書いていますが、実際にブラウザへ届けているのは前段です。どこで付けるか（アプリ／リバースプロキシ／CDN）を決めておかないと、二重に付いたり、片方の設定だけが効いていたりします。設定ファイルを読んで満足せず、必ず実際のレスポンスで確認する。当たり前のようで、前段がある環境では特に効いてくる習慣です。
      </p>

      <h2>参考にした一次情報</h2>
      <ul>
        <li><strong>RFC 9110（HTTP Semantics）</strong> 3.7節 Intermediaries（proxy / gateway＝reverse proxy / tunnel の定義）、9.3.6節 CONNECT</li>
        <li><strong>RFC 7239</strong>（Forwarded HTTP Extension）── <code>for</code> / <code>by</code> / <code>proto</code> / <code>host</code> の定義とセキュリティ考慮事項</li>
        <li><strong>RFC 9111（HTTP Caching）</strong> 1.3節（プライベート／共有キャッシュ）、3.5節（Authorization付きリクエストへの応答の保存）、4.1節（Vary）、5.1節（Age）</li>
        <li><strong>RFC 9112（HTTP/1.1）</strong> 6.3節（Transfer-Encoding が Content-Length を上書きする）、11.2節（リクエストスマグリング）</li>
        <li><strong>nginx 公式ドキュメント</strong> ngx_http_proxy_module（<code>proxy_pass</code>、<code>proxy_set_header</code> の既定値、<code>$proxy_add_x_forwarded_for</code>、<code>proxy_read_timeout</code> の既定60秒）</li>
        <li><strong>Express 公式「Express behind proxies」</strong>（<code>trust proxy</code> の設定値と警告）</li>
        <li><strong>MDN</strong>「Proxy servers and tunneling」「CONNECT」</li>
        <li><strong>実測</strong>：2026年8月30日に <code>https://secutils.jp</code> へ <code>curl</code> したレスポンスヘッダー、および同日ローカルで実行した検証コードの出力</li>
      </ul>
      <p>
        ミドルウェアの既定値は版によって変わることがあります。設定前に、お使いのバージョンの公式ドキュメントで確認してください。
      </p>

      <h2>よくある質問</h2>
      <h3>プロキシとリバースプロキシの違いは何ですか？</h3>
      <p>
        どちら側が置くかが逆です。フォワードプロキシはクライアント側が置き、クライアントの代理として外部へリクエストを送ります。リバースプロキシはサーバー側が置き、サーバーの代理としてリクエストを受け取ります。RFC 9110 はリバースプロキシを「gateway」と呼び、外向きの接続に対してはオリジンサーバーとして振る舞う中間装置と定義しています。利用者はフォワードプロキシの存在を（自分で設定するので）知っていますが、リバースプロキシの存在には普通気づきません。
      </p>

      <h3>リバースプロキシを置くと何が良いのですか？</h3>
      <p>
        TLS終端で証明書管理を1か所に集約でき、キャッシュでアプリの負荷を減らし、複数のサーバーへ負荷分散でき、パスやホスト名でルーティングでき、レート制限やセキュリティヘッダーの付与といった防御の一枚目を置けます。アプリを直接インターネットに晒さずに済むことも大きな利点です。
      </p>

      <h3>X-Forwarded-For のIPアドレスは信用できますか？</h3>
      <p>
        そのままでは信用できません。多くのプロキシは既存の値に追記する動作をするため、クライアントが最初から偽の値を付けて送ると、それが左端に残ったまま転送されます（実際に検証すると <code>1.2.3.4, 127.0.0.1</code> のようになります）。RFC 7239 も「経路上のすべてのノードが誤って、あるいは悪意を持って変更しうるため信頼できない」と警告しています。信用してよいのは自分が管理している最前段のプロキシが書いた値だけです。入口では追記ではなく上書きし、アプリ側では信頼するプロキシをIPやホップ数で限定してください。
      </p>

      <h3>502エラーと504エラーの違いは何ですか？</h3>
      <p>
        502 Bad Gateway はゲートウェイやプロキシが上流から不正な応答を受け取ったことを、504 Gateway Timeout は上流からの応答を待ってタイムアウトしたことを示します。どちらも「前段は動いていて、その奥のアプリが正常に応答できていない」という意味なので、まずアプリのプロセスと前段からの到達性、そして前段のタイムアウト設定を確認します。nginx の <code>proxy_read_timeout</code> の既定は60秒で、アプリの処理がこれを超えると、アプリ側は正常でも504が返ります。
      </p>

      <h3>プロキシとVPNはどう違いますか？</h3>
      <p>
        レイヤーが違います。プロキシはアプリケーション層で特定のプロトコル（多くはHTTP）を中継する仕組みで、URLを見て遮断するなど中身に手を加えられます。VPNはネットワーク層で通信を丸ごと包む仕組みで、中を流れるプロトコルには関知しません。ブラウザの通信だけを会社の出口経由にしたいならプロキシ、端末の通信すべてを別のネットワークに所属させたいならVPNが適しています。
      </p>

      <h2>おわりに</h2>
      <p>
        プロキシとリバースプロキシの違いは、機能の一覧を覚えるよりも<strong>「誰が置いて、誰の代理をしているか」</strong>で捉えるほうが応用が利きます。クライアントが置けばフォワードプロキシ、サーバーが置けばリバースプロキシ。それだけで、設定ファイルの場所も、障害時に最初に疑う場所も決まります。
      </p>
      <p>
        そして実務で最も事故につながりやすいのは、性能でも構成でもなく<strong>「前段を挟んだことで変わった情報」</strong>です。接続元IP、プロトコル、ホスト名、そしてキャッシュ。この4つについて「いまアプリが見ている値は誰が書いたものか」を答えられる状態にしておけば、大半のハマりどころは避けられます。手元で <code>curl -sSI</code> を打って、自分のサイトの前に何が立っているかを確かめるところから始めてみてください。
      </p>
    </ArticleLayout>
  );
}
